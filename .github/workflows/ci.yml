name: C CI

on:
  push:
  pull_request:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    - name: Build
      run: make all
    - name: Run tests
      run: make test
    - name: Test Challenge 017
      run: cd challenge07 && make test
